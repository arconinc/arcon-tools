'use client'

export default function CrmDashboardPage() {
  return (
    <div style={{ padding: '40px 32px' }}>
      <h1 style={{ fontSize: 24, fontWeight: 700, color: '#111', marginBottom: 8 }}>CRM Dashboard</h1>
      <p style={{ fontSize: 14, color: '#888' }}>Coming soon — pipeline summary, goal progress, and leaderboard.</p>
    </div>
  )
}
